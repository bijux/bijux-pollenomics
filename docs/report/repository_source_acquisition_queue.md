# Repository source acquisition queue

- Queue rows: `7`

| Source family | Priority | Current gap | Required outcome |
| --- | --- | --- | --- |
| `animal_adna` | `high` | repository supplement capture now matches visible local staging, but sample, site, and chronology extraction still lags | use the archived paper supplements to publish sample-owned identity, locality, chronology, and coordinate evidence |
| `landclim` | `medium` | collection summary still reports `landclim_site_count` as zero | rebuild the collection summary so public cross-domain counts stop understating the tracked source family |
| `landclim` | `medium` | collection summary still reports `landclim_grid_cell_count` as zero | rebuild the collection summary so public cross-domain counts stop understating the tracked source family |
| `neotoma` | `medium` | collection summary still reports `neotoma_point_count` as zero | rebuild the collection summary so public cross-domain counts stop understating the tracked source family |
| `sead` | `medium` | collection summary still reports `sead_point_count` as zero | rebuild the collection summary so public cross-domain counts stop understating the tracked source family |
| `raa` | `medium` | collection summary still reports `raa_total_site_count` as zero | rebuild the collection summary so public cross-domain counts stop understating the tracked source family |
| `raa` | `medium` | collection summary still reports `raa_heritage_site_count` as zero | rebuild the collection summary so public cross-domain counts stop understating the tracked source family |
