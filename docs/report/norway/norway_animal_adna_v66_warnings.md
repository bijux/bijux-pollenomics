# Norway animal aDNA warnings

These warnings make the country-level animal surface honest instead of
letting regional, comparator, or approximate evidence look cleaner than it is.

- `warning` `territory_projection`: `Rangifer tarandus` reaches `Norway` through an explicit territorial projection rather than a mainland-only locality.
- `warning` `comparator_scope`: `Rangifer tarandus` stays comparator-only in `Norway` and must not be promoted into domesticated-core farming support.
- `warning` `approximate_coordinates`: `Rangifer tarandus` still carries approximate or inferred coordinates in the country bundle.
- `caution` `thin_species_surface`: `Norway` currently ships a thin animal surface with only `1` represented tracked species.
