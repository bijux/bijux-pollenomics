# Sweden animal aDNA warnings

These warnings make the country-level animal surface honest instead of
letting regional, comparator, or approximate evidence look cleaner than it is.

- `caution` `no_country_resolved_animal_rows`: No tracked non-human animal locality lead is currently assignable to `Sweden` under the shipped pollenomics rules.
