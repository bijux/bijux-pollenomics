# Repository cross-domain evidence matrix

- Domain rows: `6`

| Domain | Role | Evidence units | Coverage posture | Current gap |
| --- | --- | --- | --- | --- |
| Pollen context | `primary_domain` | `landclim_site_count` 492, `landclim_grid_cell_count` 88, `neotoma_site_count` 200 | `first_class_context_family` | pollen context is strong as tracked context, but it still needs more pollenomics-first synthesis than the current atlas-facing slices provide |
| Archaeology context | `contextual_domain` | `sead_site_count` 2195, `raa_published_site_count` 761917, `raa_density_cell_count` 106 | `explicit_context_family` | archaeology context is broad but intentionally contextual; readers should not confuse it with direct pollen or sample evidence |
| Boundary framing | `framing_domain` | `country_feature_count` 4 | `strong_framing_surface` | boundary geometry is robust framing, but it should never be mistaken for scientific balance on its own |
| Fieldwork record | `contextual_domain` | `fieldwork_page_count` 2 | `narrow_honest_surface` | fieldwork is deliberately narrow and should stay explicit rather than being implied by atlas presence |
| Animal aDNA context | `contextual_domain` | `tracked_paper_count` 18, `published_atlas_point_count` 234, `unresolved_map_rows` 21 | `partial_sample_owned_surface` | animal aDNA is real and now traceable, but it is still a thinner and more recovery-bound surface than the repository's context families |
| Publication outputs | `downstream_surface` | `country_bundle_count` 4, `animal_point_count` 234 | `downstream_not_governing` | reports and atlas bundles summarize upstream evidence; they do not prove balanced coverage by themselves |
