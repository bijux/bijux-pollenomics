[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bijux-pollenomics"
version = "0.1.0"
description = "Data collection, reporting, and mapping tools for Nordic aDNA and contextual evidence layers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "build>=1.2.2",
    "mkdocs>=1.6.1",
    "mkdocs-autorefs>=1.4.2",
    "mkdocs-git-revision-date-localized-plugin>=1.4.7",
    "mkdocs-material>=9.6.12",
    "pymdown-extensions>=10.15",
    "ruff>=0.13.0",
]

[project.scripts]
bijux-pollenomics = "bijux_pollenomics.cli:main"
bijux-pollenomics-aadr = "bijux_pollenomics.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
